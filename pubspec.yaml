name: instagram_clone_flutter
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0" # تم تحديثه ليتوافق مع Firebase الحديثة وFlutter 3.x

dependencies:
  flutter:
    sdk: flutter

  # Flutter UI
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.10
  flutter_staggered_grid_view: ^0.7.0

  # Firebase & Backend
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.4
  cloud_firestore: ^4.15.4
  firebase_storage: ^11.6.4

  # أدوات وتنسيق ورفع صور
  image_picker: ^1.0.7
  intl: ^0.19.0
  provider: ^6.1.2
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/ic_instagram.svg
